<div class="pax-navigation">
        <!-- Loop through the navigation items -->
        {{#foreach navigation}}
        <div class="nav-{{slug}}{{#if current}} nav-current{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></div>
        {{/foreach}}
        <!-- End the loop -->
        {{search}}
</div>